{
  "name": "jump-extension",
  "version": "0.6.2",
  "private": true,
  "packageManager": "npm@12.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "npm run icons && tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "icons": "cd logo && npx tsx create-logo.ts",
    "lint": "eslint src/",
    "lint:fix": "eslint --fix src/",
    "format": "prettier --write src/",
    "format:check": "prettier --check src/",
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "npm test && npm run build && cd dist && zip -r ../jump-extension.zip . -x '**/.DS_Store' -x '.DS_Store'",
    "build:firefox": "npm run icons && tsc --noEmit && BROWSER=firefox vite build && cp public/manifest.firefox.json dist/manifest.json",
    "package:firefox": "npm test && npm run build:firefox && cd dist && zip -r ../jump-extension-firefox.zip . -x '**/.DS_Store' -x '.DS_Store'"
  },
  "dependencies": {
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@resvg/resvg-js": "^2.6.2",
    "@types/chrome": "^0.2.7",
    "@types/node": "^26.4.0",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.1.1",
    "eslint": "^10.0.3",
    "eslint-config-prettier": "^10.1.8",
    "opentype.js": "^2.0.0",
    "prettier": "^3.8.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.57.0",
    "vite": "^8.2.2",
    "vitest": "^4.1.0"
  }
}
